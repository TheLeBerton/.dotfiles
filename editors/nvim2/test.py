

def main() -> None:
    a: int = 10
    b: int = 5
    print(f"{ a } + { b } = { add( a, b ) }")
    print(f"{ a } - { b } = { sub( a, b ) }")
    print(f"{ a } * { b } = { mul( a, b ) }")
    print(f"{ a } / { b } = { division( a, b ) }")


main()
